To run the the code plesae run

```pip install torch torchvision matplotlib numpy
python tagger3.py```

in line 6 of the code there is a boolean variable called pos that dictates whether we are running pos or ner (true for pos, false for ner)
the needed files should be in this hierarchy:
├── wordVectors.txt
├── vocab.txt
├── ner
│   ├── dev
│   ├── test
│   └── train
├── pos
│   ├── dev
│   ├── test
│   └── train
└── Part4
    ├── utils.py
    └── tagger3.py
The code will output new files test4.ner and test4.pos (rewrite if ones already exist)
and 2 images Dev Losses {ner/pos} and Dev Accuracies {ner/pos} and pre trained word vectors {ner/pos}
showing loss and accuracy over epochs.